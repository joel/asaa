#!/usr/bin/env bash

source ./bin/init-env

docker-compose run --rm wait-middleware

docker-compose run --rm \
  -e DB_PORT=${DB_PORT} \
  -e DB_HOST=${DB_HOST} \
  -e DB_USER=${DB_USER} \
  -e DB_PASSWORD=${DB_PASSWORD} \
  -e HUB_URL=${HUB_URL} \
  console bash -c "RAILS_ENV=test bin/rails test:system"

<% method = attachment.persisted? ? :patch : :post %>
<%= form_with(scope: :attachment, url: polymorphic_url([@behaveable, attachment]), method: method, local: true) do |form| %>
  <% if attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
    <%= form.file_field :attachment, direct_upload: true %><br>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
